{
    "$schema": "https://turbo.build/schema.json",
    "daemon": false,
    "globalDependencies": [
        "packages/plugin/**"
    ],
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        },
        "dev": {
            "cache": false
        }
    }
}
